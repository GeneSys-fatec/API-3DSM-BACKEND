package com.servico_comentario.servico_comentario.model.dto;

// Representa o Usuário logado da forma mais simples possíovel
public record UsuarioPrincipalDTO(
        String id,
        String nome
) {
}